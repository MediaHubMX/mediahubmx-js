{
  "name": "@mediahubmx/test-utils",
  "description": "Test utils for MediaHubMX addons",
  "version": "1.0.0",
  "main": "dist",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "prepublishOnly": "rm -rf dist && npm run build",
    "develop-addon": "ts-node-dev addon"
  },
  "dependencies": {
    "@mediahubmx/sdk": "^1.0.0",
    "jest": "^26.6.3"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "supertest": "^6.1.3",
    "ts-jest": "^26.5.5",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
