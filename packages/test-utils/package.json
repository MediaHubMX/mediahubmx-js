{
  "name": "@mediahubmx/test-utils",
  "description": "Test utils for MediaHubMX addons",
  "version": "1.0.5",
  "main": "dist",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "prepublishOnly": "rm -rf dist && npm run build",
    "develop-addon": "ts-node-dev addon"
  },
  "dependencies": {
    "@mediahubmx/sdk": "^1.0.5",
    "jest": "^28.1.3"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "@types/supertest": "^2.0.12",
    "supertest": "^6.2.4",
    "ts-jest": "^28.0.7",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
